# Mobile Demo

A deliberately broken mobile checkout app for the Trace investigation demo.

The initial `main` branch contains a mobile-only checkout CTA bug:

- Open the app at a mobile viewport such as `390x844`.
- Apply coupon `SAVE20`.
- The discount applies, but the checkout CTA disappears on mobile.

Trace is expected to open an issue, push a fix branch, and create a pull request.
